var Backbone = {
    _: 'underscore',
    $: 'jQuery || Zepto',
    Events: {
        on: fn,
        off: fn,
        once: fn,
        trigger: fn,
        listenTo: fn,
        listenToOnce: fn
    }
};
